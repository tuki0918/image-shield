{
  "name": "@image-shield/cli",
  "version": "0.8.1",
  "description": "CLI implementation of image fragmentation and restoration (RESERVED)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "image-shield": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "echo 'CLI implementation not yet available'",
    "build:check": "echo 'CLI implementation not yet available'", 
    "test": "echo 'CLI implementation not yet available'"
  },
  "dependencies": {
    "@image-shield/core": "file:../core",
    "@image-shield/node": "file:../node"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.1.4"
  }
}